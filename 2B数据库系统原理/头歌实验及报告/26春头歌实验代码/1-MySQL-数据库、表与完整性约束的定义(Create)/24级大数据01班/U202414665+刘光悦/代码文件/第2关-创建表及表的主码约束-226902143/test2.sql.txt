# 请在以下适当的空白处填写SQL语句，完成任务书的要求。空白行可通过回车换行添加。 
CREATE DATABASE TestDb;

-- 2. 切换到 TestDb 数据库
USE TestDb;

-- 3. 创建表 t_emp
CREATE TABLE t_emp (
  id      INT        NOT NULL,
  name    VARCHAR(32),
  deptId  INT,
  salary  FLOAT,
  PRIMARY KEY (id)
);



/* *********** 结束 ************* */